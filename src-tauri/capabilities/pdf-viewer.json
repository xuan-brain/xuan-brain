{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pdf-viewer",
  "description": "Capability for PDF viewer windows",
  "windows": ["pdf-viewer-*"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-close"
  ]
}
