import { Typography } from "antd";

export default function SettingsPage() {
  return (
    <div style={{ padding: 24 }}>
      <Typography.Title level={2}>设置</Typography.Title>
      <Typography.Paragraph>这里是应用设置。</Typography.Paragraph>
    </div>
  );
}
